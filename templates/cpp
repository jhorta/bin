#include <iostream>

using namespace std;

int main(int argc, char * argv[]) {
  if(argc == 1) {
    cerr << "USAGE " << argv[0] << " incorrect number of arguments\n";
    return -1;
  }
  return 0;
}
